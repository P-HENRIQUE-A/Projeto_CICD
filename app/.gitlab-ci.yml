image: docker:latest

services:
  - docker:dind

stages:
  - build
  - push

variables:
  DOCKER_DRIVER: overlay2

before_script:
  - docker login -u $DOCKER_USER -p $DOCKER_PASSWORD

build:
  stage: build
  script:
   - docker build -t henrique150785/projeto-cicd:latest .

push:
  stage: push
  script:
  - docker build -t henrique150785/projeto-cicd:latest .